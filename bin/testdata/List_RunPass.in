
list := object() <- load("./list.bn");
mylist := list.new();
println(mylist.length().to_string());
mylist.append("hello");
mylist.append("world");
mylist.append("bone");
println(mylist.length().to_string());

mylist.foreach(def(data)() {
    println(data);
});