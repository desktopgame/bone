fio := object() <- load("./file.bn");
println("load");
newfile := object() <- fio.open("./test2.out", "w");
if(newfile.error) {
    println("error");
    return;
}
newfile.file.puts("puts puts puts ...");
newfile.file.close();
