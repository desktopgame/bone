fio := object() <- load("./file.bn");
println("load");
newfile := object() <- fio.open("./test2.out", "r");
if(newfile.error) {
    println("error");
    return;
}
println(newfile.file.gets());
newfile.file.close();
