#ifndef BONE_CONFIG_H
#define BONE_CONFIG_H

#define REVISION ("${REVISION}")
#define BUILD_VERSION ("${BONE_BUILD_VERSION} rev.${REVISION}")
#cmakedefine HAVE_UNISTD @HAVE_UNISTD@
#cmakedefine HAVE_DLFCN @HAVE_DLFCN@
#cmakedefine HAVE_WINDOWS @HAVE_WINDOWS@

/**
 * @return
 */
const char* bnGetBuildVersion();
#endif