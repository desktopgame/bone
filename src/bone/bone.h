#ifndef BONE_BONE_H
#define BONE_BONE_H

#include <assert.h>
#include <stdbool.h>
#include <stdio.h>
#include "config.h"
#include "glib.h"
#include "util/fmt.h"
#include "util/io.h"
#include "util/memory.h"
#include "util/string_pool.h"

#define BN_MAX(a, b) (a > b ? a : b)
#define BN_MIN(a, b) (a < b ? a : b)
#endif